// Import required Scene class from three.js
import { Scene } from "../three.module.js";

function createScene() {
  // Create new three.js scene
  const scene = new Scene();

  return scene;
}

export { createScene };
